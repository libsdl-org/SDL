This is a small SDLGPU application which simulates asteroids orbiting around Earth.
It uses compute shaders to calculate each asteroid's position and velocity, and instancing to render them.

Press left/right to decrease/increase timestep, and up/down to increase/decrease the asteroid count.
